<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">                    
        <div class="navbar-header">
            <button aria-expanded="false" class="navbar-toggle collapsed" data-target="#bs-example-navbar-collapse-1" data-toggle="collapse" type="button">
                <span class="sr-only">
                    Toggle navigation
                </span>
                <span class="icon-bar">
                </span>
                <span class="icon-bar">
                </span>
                <span class="icon-bar">
                </span>
            </button>
            <a class="navbar-brand" href="/#!/home">
                <img src="images/logo-blue-bank.png" alt="Banco Blue Bank" title="Banco Blue Bank">
            </a>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <div class="nav navbar-nav">
                <ul class="align-middle navbar-nav">
                    <li ng-class="{'active' : paginaAtual == '/extrato'}">
                        <a href="/#!/extrato">
                            EXTRATO
                            <span class="sr-only" ng-if="paginaAtual == '/extrato'">
                                (current)
                            </span>
                        </a>
                    </li>
                    <li ng-class="{'active' : paginaAtual == '/transferencia'}">
                        <a href="/#!/transferencia">
                            TRANSFERÊNCIA
                        </a>
                        <span class="sr-only" ng-if="paginaAtual == '/transferencia'">
                            (current)
                        </span>
                    </li>
                </ul>    
            </div>
            
            <div class="navbar-right">
                <% if (typeof req.session.authenticated == 'undefined') { %>
                    <a class="btn btn-default btn-entrar" href="/#!/login">Acesse sua conta</a>
                <% } else { %>
                    <a class="btn btn-default btn-entrar" href="cliente/logout">Sair</a>
                <% } %>
            </div>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
<div class="ui-view"></div>
<div class="container-fluid">
    <div class="row">
        <div class="col-xs-12 rodape text-center">
            <img src="images/logo-rodape-blue-bank.png" alt="Blue Bank" title="Blue Bank">
            <p><%= sails.moment().format("YYYY") %> - Todos os direitos reservados</p>
        </div>
    </div>
</div>
<div class="container-fluid">
    <div class="row"></div>
</div>